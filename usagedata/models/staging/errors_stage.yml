version: 2
sources:
  - name: "{{var('source')}}"
    schema: "{{var('sf_src_schema_name')}}"
    tables: 
      - name: "{{var('sf_tbl_errors')}}"
        columns: 
            - name: ERROR_TYPE
              data_type: VARCHAR(0, 0)
              quote: true

            - name: ERROR_CODE
              data_type: VARCHAR(0, 0)
              quote: true

            - name: DESCRIPTION
              data_type: VARCHAR(0, 0)
              quote: true
