version: 2
sources:
  - name: "{{var('source')}}"
    schema: "{{var('sf_src_schema_name')}}"
    tables: 
      - name: "{{var('sf_tbl_roles')}}"
        columns: 
            - name: NAME
              data_type: VARCHAR(0, 0)
              quote: true

            - name: COMMENT
              data_type: VARCHAR(0, 0)
              quote: true

            - name: CREATED_ON
              data_type: VARCHAR(0, 0)  
              quote: true

            - name: DELETED_ON
              data_type: VARCHAR(0, 0)
              quote: true