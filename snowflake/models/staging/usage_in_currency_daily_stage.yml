version: 2
sources:
  - name: "{{var('source')}}"
    database: "{{var('source')}}"
    schema: "{{var('sf_src_schema_name_ou')}}"
    tables: 
      - name: "{{var('sf_tbl_usage_curr_daily')}}"
        description: 
        columns: 
            - name: ORGANIZATION_NAME
              data_type: VARCHAR(0, 0)
              description:
              quote: true
            
            - name: ACCOUNT_NAME
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: REGION
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: SERVICE_LEVEL
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: USAGE_DATE
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: USAGE_TYPE
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: CURRENCY
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: USAGE
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: USAGE_IN_CURRENCY
              data_type: VARCHAR(0, 0)
              description:
              quote: true

            - name: BALANCE_SOURCE
              data_type: VARCHAR(0, 0)
              description:
              quote: true