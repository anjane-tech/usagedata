version: 2
sources:
  - name: "{{var('source')}}"
    schema: "USAGEDATA_{{var('usage_data_staging_schema_name')}}"
    tables: 
      - name: "{{var('sf_tbl_warehouse')}}"
        columns: 
            - name: WAREHOUSE_NAME
              data_type: VARCHAR(0, 0)
              quote: true

            - name: SIZE
              data_type: VARCHAR(0, 0)
              quote: true

            - name: TYPE
              data_type: VARCHAR(0, 0)
              quote: true

            - name: OWNER
              data_type: VARCHAR(0, 0)
              quote: true

            - name: MIN_CLUSTER_COUNT
              data_type: VARCHAR(0, 0)
              quote: true

            - name: MAX_CLUSTER_COUNT
              data_type: VARCHAR(0, 0)
              quote: true

            - name: CREATED_ON
              data_type: VARCHAR(0, 0)
              quote: true

            - name: UPDATED_ON
              data_type: VARCHAR(0, 0)
              quote: true

            - name: AUTO_SUSPEND
              data_type: VARCHAR(0, 0)
              quote: true

            - name: AUTO_RESUME
              data_type: VARCHAR(0, 0)
              quote: true

            - name: UUID
              data_type: VARCHAR(0, 0)
              quote: true
              