version: 2
sources:
  - name: "{{var('source')}}"
    database: "{{var('source')}}"
    schema: "{{var('sf_src_schema_name')}}"
    tables: 
      - name: "{{var('sf_tbl_metering_history')}}"
        description: "N/A"
        columns: 
            - name: SERVICE_TYPE
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true
            
            - name: START_TIME
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true
            
            - name: END_TIME
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: ENTITY_ID
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: NAME
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: CREDITS_USED_COMPUTE
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true
            
            - name: CREDITS_USED_CLOUD_SERVICES
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: CREDITS_USED
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: BYTES
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: ROWS
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true

            - name: FILES
              data_type: VARCHAR(0, 0)
              description: "N/A" 
              quote: true
