version: 2

models:
  - name: dim_replication_usage_history
    columns:
          - name: ORGANIZATION_NAME
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: ACCOUNT_NAME
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: ACCOUNT_LOCATOR
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: REGION
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: USAGE_DATE
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: DATABASE_ID
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: DATABASE_NAME
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: CREDITS_USED
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

          - name: BYTES_TRANSFERED
            data_type: VARCHAR(0, 0)
            description:
            quote: true
            tests:
              - not_null

