version: 2
seeds: 
    - name: errors
      config:
        +schema: seed
        alias: errors_csv
        column_types:
                ERROR_TYPE: VARCHAR
                ERROR_CODE: VARCHAR
                DESCRIPTION: VARCHAR